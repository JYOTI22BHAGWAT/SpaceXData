# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SpaceXApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SpaceXApp
      pod 'Alamofire'
      pod 'SwiftyJSON'
      pod 'JGProgressHUD'
      pod 'KSToastView'
      pod "RappleProgressHUD"
      pod 'Kingfisher', '~> 5.0'
      pod 'ImageSlideshow', '~> 1.8.3'
  target 'SpaceXAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SpaceXAppUITests' do
    # Pods for testing
  end

end
